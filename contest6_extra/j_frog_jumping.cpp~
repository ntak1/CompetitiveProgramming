#include<cstdio>

using namespace std;

int main(){
    int t;
    long long int a, b, k;

    scanf("%d", &t);
    while(t--){
        scanf("%lld %lld %lld", &a, &b, &k);
        printf("%lld\n", (k/2)*(a-b) + (k%2)*a);
    }
    return 0;
}
